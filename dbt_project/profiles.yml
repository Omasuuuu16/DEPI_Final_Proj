student_health:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Update the default fallback to the Docker path
      path: "{{ env_var('DUCKDB_PATH', '/opt/airflow/data/student_health.duckdb') }}"
      schema: main
      threads: 4